services:
  api:
    environment:
      FINSIGHT_ENV: production
      FINSIGHT_ALLOWED_ORIGINS: ${FINSIGHT_ALLOWED_ORIGINS:-https://your-domain.com}
      FINSIGHT_ALLOWED_HOSTS: ${FINSIGHT_ALLOWED_HOSTS:-your-domain.com,localhost,127.0.0.1,api}

  frontend:
    ports:
      - '127.0.0.1:3000:80'
